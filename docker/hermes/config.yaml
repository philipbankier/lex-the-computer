# Hermes Agent — default Lex configuration
# Overrides via environment variables take precedence.

model: claude-sonnet-4-6

terminal:
  backend: docker

approvals:
  mode: "off"

compression:
  threshold: 0.7

api_server:
  enabled: true
  host: "0.0.0.0"
  port: 8642
